package com.atguigu.demo;

import javax.xml.stream.events.Comment;
import java.util.List;

/*
entity: user , blog
function : write ,comment , answer
 */
/*
public class DesignBlog {

    class User {
        user name;
        List<blog>
        List<Comment>
    }
    class Blod{
        List<Comment>
    }
    class Comment{
        user
        blog
        flag (0 -- comment, 1--answer)
        msg
    }
    main(){
        user = new user();
        blod = new blod();
        //add property
        user.blodList.add(blod)
        comment  = new comment()
        comment.setUser(user)
                // you need to set value
         comment.setBlod(blod)
        user.commentList.add(comment)
    }
}

 */
